import { User } from './models/User';

const user = new User({ name: 'myname', age: 100});

user.on('change', () => {

});

user.on('change', () => {

});

user.on('sgfsg', () => {

});

console.log(user);
